## Презентации

### PostgreSQL

По умолчанию backend подключается к стандартной базе `postgres`:

```powershell
.\gradlew.bat bootRun
```

Liquibase сам создаст schema `presentation` и необходимые таблицы внутри этой базы.
